# x86-64-v3 = AVX2 + BMI2 + POPCNT (Haswell/Zen1 and newer). Bitboard engines lean
# heavily on popcnt/tzcnt; the generic x86-64 baseline emulates them in software.
# Use `RUSTFLAGS="-C target-cpu=native"` for a machine-specific build.
[build]
rustflags = ["-C", "target-cpu=x86-64-v3"]
