name: Release binaries

# Builds the release binaries and attaches them to the release for a tag. Run it from the
# Actions tab (or it fires when a v* tag is pushed). The network is not in this repository,
# so each job downloads it from Peras-networks first.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build and upload to (e.g. v3.0.0)"
        required: true

permissions:
  contents: write

env:
  NET_URL: https://github.com/FirePlank/Peras-networks/releases/download/peras-v4/peras-v4.nnue

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: windows-x86-64-avx2
            os: windows-latest
            target: x86_64-pc-windows-msvc
            cpu: x86-64-v3
          - name: windows-x86-64
            os: windows-latest
            target: x86_64-pc-windows-msvc
            cpu: x86-64-v2
          - name: linux-x86-64-avx2
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            cpu: x86-64-v3
          - name: linux-x86-64
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            cpu: x86-64-v2
          - name: macos-arm64
            os: macos-latest
            target: aarch64-apple-darwin
            cpu: apple-m1
          # macos-13 (the last Intel-hosted runner) is retired; cross-compile this
          # slice from the arm64 runner instead, since the Xcode toolchain it ships
          # carries both SDKs.
          - name: macos-x86-64-avx2
            os: macos-latest
            target: x86_64-apple-darwin
            cpu: x86-64-v3
          # Statically linked against musl, so it also runs on Android, whose libc is not
          # glibc. Built on an arm64 runner so the tests and bench run natively. rust-lld
          # links it without a musl C toolchain, which the engine can do because it has no
          # C dependencies.
          - name: linux-aarch64-musl
            os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-musl
            cpu: generic
            rustflags: -C linker=rust-lld

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Fetch the network
        shell: bash
        run: |
          mkdir -p nets
          curl -sSfL "$NET_URL" -o nets/peras.nnue
          ls -l nets/peras.nnue

      - name: Build
        shell: bash
        # RUSTFLAGS overrides the target-cpu in .cargo/config.toml, which is x86-only.
        run: RUSTFLAGS="-C target-cpu=${{ matrix.cpu }} ${{ matrix.rustflags }}" cargo build --release --target ${{ matrix.target }}

      - name: Test and bench
        shell: bash
        run: |
          RUSTFLAGS="-C target-cpu=${{ matrix.cpu }} ${{ matrix.rustflags }}" cargo test --release --target ${{ matrix.target }}
          BIN=target/${{ matrix.target }}/release/peras
          [ -f "$BIN.exe" ] && BIN="$BIN.exe"
          "$BIN" bench 12

      - name: Package
        shell: bash
        run: |
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          DIR="peras-${TAG#v}-${{ matrix.name }}"
          mkdir "$DIR"
          BIN=target/${{ matrix.target }}/release/peras
          if [ -f "$BIN.exe" ]; then cp "$BIN.exe" "$DIR/peras.exe"; else cp "$BIN" "$DIR/peras"; fi
          cp README.md CHANGELOG.md LICENSE "$DIR/"
          if [ "${{ runner.os }}" = "Windows" ]; then
            7z a -tzip "$DIR.zip" "$DIR" > /dev/null && echo "ASSET=$DIR.zip" >> "$GITHUB_ENV"
          else
            tar czf "$DIR.tar.gz" "$DIR" && echo "ASSET=$DIR.tar.gz" >> "$GITHUB_ENV"
          fi

      - name: Upload to the release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          # A tag push starts this before any release exists, and uploading to a missing
          # release fails. All six jobs reach here at once, so whichever loses the race to
          # create it carries on rather than failing.
          #
          # The body below is the part that never changes between releases. The one-line
          # summary above it is written by hand afterwards, with `gh release edit`.
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            cat > notes.md <<'NOTES'
          ## Which binary

          The network is compiled into every binary, so each archive is self-contained.

          | Archive | For |
          | ------- | --- |
          | `windows-x86-64-avx2`, `linux-x86-64-avx2` | Haswell/Zen 1 and newer. Pick this unless your CPU predates 2013 |
          | `windows-x86-64`, `linux-x86-64` | Any x86-64 with SSE4.2. About a quarter slower |
          | `macos-arm64` | Apple Silicon |
          | `macos-x86-64-avx2` | Intel Macs |
          | `linux-aarch64-musl` | arm64 Linux, and Android via a terminal app. Static, so it needs nothing installed |
          NOTES
            gh release create "$TAG" --title "$TAG" --notes-file notes.md || true
          fi
          gh release upload "$TAG" "$ASSET" --clobber
